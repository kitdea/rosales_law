import { type SchemaTypeDefinition } from "sanity";

/**
 * Registry of Sanity document and object schemas.
 * Add schema definitions here as the CMS content model is built out
 * (e.g. settings, practiceArea, matter, insight).
 */
export const schema: { types: SchemaTypeDefinition[] } = {
  types: [],
};
