import type { Metadata } from "next";
import { Fraunces, Inter } from "next/font/google";
import "./globals.css";

const fraunces = Fraunces({
  subsets: ["latin"],
  weight: ["400", "500", "600"],
  variable: "--font-fraunces",
  display: "swap",
});

const inter = Inter({
  subsets: ["latin"],
  weight: ["400", "500", "600"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "Rosales Law Firm — Clear counsel. Decisive action.",
  description:
    "Full-service legal counsel for Filipino businesses and families. Corporate, litigation, family & estate, labor & immigration. Book a consultation.",
  metadataBase: new URL("https://rosaleslaw.ph"),
  openGraph: {
    title: "Rosales Law Firm",
    description:
      "Full-service legal counsel for Filipino businesses and families.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${fraunces.variable} ${inter.variable}`}>
      <body>{children}</body>
    </html>
  );
}
